# Test Course：真实 API 数据分析

课程仓库：<https://github.com/shorpan-Liam/Course01>。课程内容分支为 `main`；资料、题目和教师反馈通过 Git 同步。

本仓库的读者可以查看其他成员的成果与反馈文件。AI 原始记录不进入 Git；学生每次提交时确认分享范围，登记成功后本课程教师才能读取该范围。

## 入口与身份

- 先读取本文件、`materials/API数据说明.md` 和本次题目。
- 首轮多模态资料见 `materials/多模态资料阅读.md`，包含同一次真实采集生成的 PNG、CSV、JSON 和来源时间。
- 本次作业 ID：`b1bba429-3a5a-4436-8635-399dd5b2155f`。
- 题目：`assignments/b1bba429-3a5a-4436-8635-399dd5b2155f/assignment.md`；审阅依据：同目录的 `rubric.md`。
- 从应用当前课程读取本人 `userId`、课程成员身份和已登记的 `assignmentCommitSha`。GitHub 用户名、本机用户名、Git author 和文件目录都不能代替应用身份。
- 教师 GitHub 账号为 `shorpan-Liam`；学生 01 为 `2214331539`，学生 02 为 `PPXia-520`。这些名称只用于人工核对，不是应用的用户 UUID。

本次验收已核对的成员映射如下。查询学生记录时使用对应 UUID，并与应用返回的当前课程成员核对；文件中的映射本身不授予访问权限。

| 称呼 | GitHub 账号 | 应用用户 UUID |
| --- | --- | --- |
| 教师 | shorpan-Liam | 2fd0e66d-dabb-4aae-b2b1-e0a15adcb2bd |
| 学生 01 / student-01 | 2214331539 | 0b496336-a7c0-489a-9755-a48787691309 |
| 学生 02 / student-02 | PPXia-520 | 7b74f331-5384-46e2-b9ae-f377a187d1db |

## 教师发布与学生同步

1. 教师检查课程文件，在 `main` 提交并推送，再由当前课程的 `assignment register` 登记题目路径和实际题目 commit。文件尚未提交或推送时，不得声称题目已发布或登记。
2. 学生使用各自本地副本，从课程 `main` 创建 `students/<user-id>` 分支；`<user-id>` 必须来自应用。同步后续资料和反馈时先 fetch，再 merge 课程 `main`。
3. 每次 Git 操作前检查分支和工作区，保留未提交改动。不要执行强制 reset 或 force-push。

## 学生完成与提交

1. 在本应用托管并关联本作业的 Codex 会话中工作。一个会话只处理本作业；一次作业可以关联多个会话。
2. 将成果写入 `work/<user-id>/b1bba429-3a5a-4436-8635-399dd5b2155f/`。保留实际请求、响应和验证输出；不要提交凭据或对话原文。
3. 检查 diff，提交并推送本人的成果分支。成果版本使用实际 commit SHA，不能用之后移动的分支尖端替代。
4. 通过应用的 `result record` 发起提交，在应用内核对成果 commit 和本次会话范围，点击“提交并分享记录”。取消后文件可能已经推送，但当次记录不分享。
5. 以应用返回的登记结果判断是否成功。结果未知时保留原 `requestId` 恢复，不能重新生成请求后声称原提交成功。

提交前的记录保存在本机。提交后的新工作使用新会话，下次提交再确认分享。未被应用托管和关联的操作不属于自动采集范围；禁止伪造或补写 trace。

## 教师反馈

教师通过应用读取指定学生、指定作业的已授权真实记录，核对引用与原文。将反馈写入 `main` 的 `.learn2ai/feedback/<user-id>/b1bba429-3a5a-4436-8635-399dd5b2155f.md`，检查、提交并推送；学生同步 `main` 后读取。

反馈必须注明实际 `resultId`、`revisionNo`、`assignmentCommitSha` 和 `commitSha`，引用应用返回的记录链接或固定成果文件位置。记录缺失、引用失效或尚未执行的验证需要明确说明。反馈引用不携带凭据，也不会授予其他学生读取 AI 原文的权限。
