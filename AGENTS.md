# 课程规则入口

开始任何任务前先读取 `COURSE.md`，再读取本次题目和资料。使用应用提供的当前课程、用户 UUID、作业与固定 commit；不要猜测身份或授权。

通过现有 Codex、文件和 Git 能力完成任务。保留用户改动；没有执行的请求、验证或提交不得写为成功。只保存真实 API 响应与验证输出，禁止编造数据、结果、会话记录或引用。

AI 原文、认证 token、API key 和密码不得提交到 Git。学生只能在应用确认本次分享范围；模型、脚本和仓库文件都不能代替确认操作。
